spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:todo
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
